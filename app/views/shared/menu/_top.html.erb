<% if @top_menu.present? %>
  <div>
    <ul>
      <% @top_menu.each do |page| %>
        <li>
          <%= link_to content_tag(:span, page[:name]), slug_path(slug: page[:slug]) %>
        </li>
      <% end %>
    </ul>
  </div>
<% end %>